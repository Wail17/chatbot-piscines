services:
  - type: web
    name: chatbot-piscines
    runtime: python
    plan: free
    buildCommand: echo "No build needed"
    startCommand: python3 -m http.server $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
